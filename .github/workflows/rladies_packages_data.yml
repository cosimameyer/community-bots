name: Update package metadata for RLadies+

on:
  schedule:
    - cron: '0 1 * * 2'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: checkout repo content
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.SECRET_WRITE }}

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version-file: 'pyproject.toml'

      - name: install python packages
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Execute py script
        env:
          BASE_URL: "https://github.com/rladies/awesome-rladies-creations/tree/main/data/packages"
          GITHUB_RAW_URL: "https://raw.githubusercontent.com/rladies/awesome-rladies-creations/main/data/packages"
          JSON_FILE: "metadata/rladies_packages_meta_data.json"
        run: python src/get_packages_data.py

      - name: Push changes
        id: cpr
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.SECRET_WRITE }}
          commit-message: "Update RLadies package metadata"
          branch: bot/rladies-packages-metadata
          delete-branch: true
          title: "Update RLadies package metadata"
          body: "Automated update of RLadies package metadata."

      - name: Auto-merge PR
        if: steps.cpr.outputs.pull-request-number != ''
        env:
          GH_TOKEN: ${{ secrets.SECRET_WRITE }}
        run: gh pr merge --squash "${{ steps.cpr.outputs.pull-request-number }}" -R cosimameyer/community-bots
